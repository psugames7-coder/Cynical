org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Loom must be new enough for the Minecraft version below. If the build fails
# during configuration saying it cannot find this plugin version, that is the
# first thing to change.
loom_version=1.11-SNAPSHOT

# Bump these together when you move Minecraft version.
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.16.10
fabric_version=0.115.0+1.21.11

mod_version=1.2.0
maven_group=com.kephale
archives_base_name=deathban
